
import React from 'react';
import { ScrollView, StyleSheet, Text, View } from 'react-native';

const ViewFeeReceipt = ({ navigation }) => {

    return (
        <ScrollView>
            <View >
                <Text>View Fee Receipt</Text>
            </View>
        </ScrollView>
    )
}

export default ViewFeeReceipt

const styles = StyleSheet.create({})